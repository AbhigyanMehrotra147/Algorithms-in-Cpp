#include <iostream>
#include <vector>
using namespace std;
class Vec_try
{
    vector<vector<int>> assign(int M, int N)
    {
        vector<int> temp1(M, -1);
        vector<vector<int>> temp2(N, temp1);
    }
}